{
  "name": "asmbli-penpot-plugin",
  "version": "0.1.0",
  "description": "Asmbli plugin for programmatic Penpot control via MCP",
  "main": "dist/plugin.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "bundle": "esbuild src/plugin.ts --bundle --outfile=dist/plugin.js --format=iife --global-name=asmbliPlugin"
  },
  "keywords": ["penpot", "plugin", "asmbli", "mcp"],
  "author": "Asmbli Team",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "^0.19.0"
  }
}
